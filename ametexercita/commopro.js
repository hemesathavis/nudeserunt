function extendOffset(value, offset) {
  // Assuming both value and offset are numbers
  return value + offset;
}

// Example usage:
const initialValue = 10;
const offsetValue = 5;
const result = extendOffset(initialValue, offsetValue);

console.log(result); // Output: 15
